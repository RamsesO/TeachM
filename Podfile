# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TeachM' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TeachM

    pod 'Firebase/Core'
    pod 'Firebase', '>= 2.5.0'
    pod 'Firebase/Database'
  target 'TeachMTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Firebase/Core'
    pod 'Firebase', '>= 2.5.0'
    pod 'Firebase/Database'
  end

  target 'TeachMUITests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Firebase/Core'
    pod 'Firebase', '>= 2.5.0'
    pod 'Firebase/Database'
  end

end
